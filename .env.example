# Baked into the JS bundle at Vite build time.
# For Docker: set in your shell before running docker compose up --build
# Example: export API_BASE_URL=https://api.yourdomain.com
API_BASE_URL=http://localhost:3000
